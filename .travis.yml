language: python
python:
  - "3.6"
install:
  - pip install codecov pytest-cov
  - pip install --quiet .
script:
    - pytest --cov=mcqa tests/
    - pylint
after_success:
  - codecov
  - pip install mcqa
